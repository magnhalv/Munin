import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.munin.mobileapp',
  appName: 'Munin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
